export class Vec2 {
    x: number;
    y: number;

    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
};


// export function dot(a: Vec2, b: Vec2) {
//     let p = 0;

//     p = p + a.x * b.x;
//     p = p + a.y * b.y;

//     return p;
// }